# 开发计划：WMO 城市气候数据解析与图表生成工具

> 状态：**已完成**（P0–P9 全部实施并通过测试；本文件保留作为设计依据与决策记录）
> 数据源：世界天气信息服务网（WMO） https://worldweather.wmo.int/
> 实际交付：`README.md`（使用说明）、`config/README.md`（配置项完整字典）、`output/`（示例产出）
> 目录说明：本文件中的目录为**开发期规划**，不反映当前布局。运行产物已统一归档到项目根：
> `output/`（交付物）、`cache/`（运行缓存）、`logs/`（运行日志）、`tests/_output/`（测试产物）；
> `过程文件/` 仅保留开发过程留证（侦察证据）。

## 已确认的技术决策（2026-09-16）

| 决策点 | 结论 |
|---|---|
| 绘图方案 | 仅 matplotlib 静态图（PNG / SVG / PDF） |
| 配置格式 | JSON 为主 + 独立配置项字典文档 |
| 表格输出 | CSV + Markdown + XLSX（另附 JSON） |
| 附加能力 | 城市名反查 cityId、一次批量多城市、多城市对比图、数据本地缓存（**全部实现**） |

### 实施结果与计划的差异

1. 新增 **第三轴（`axes_tertiary`）**：实测发现「降水日数」与「降水量」量级差距过大（天 vs 毫米），
   同轴显示时降水日数会被压成一条平线，因此增设带外偏移的第三轴来并置显示。
2. 新增 `--set` 点路径覆盖与 `config/profiles.yaml` 的 `styles` / `extends` 机制，配置复用更彻底。
3. 新增 `tests/fixtures/` 离线夹具（9 个真实样本），使回归测试不依赖网络。
4. 边界样本比预期更丰富：发现「降水全缺」「气温全缺」「降水日数全缺」三类城市，
   均纳入测试与自动降级逻辑（`data.auto_disable_empty_series`）。

---

## 一、侦察结论（已验证，非推测）

| 项 | 实测结论 |
|---|---|
| 页面地址 | `https://worldweather.wmo.int/zh/city.html?cityId={cityId}` |
| 页面是否含数据 | **不含**。页面 HTML 仅含 JS 渲染逻辑，气候数值由前端异步加载 |
| 数据接口 | `https://worldweather.wmo.int/zh/json/{cityId}_zh.xml`（实为 **JSON**，扩展名误导） |
| 接口响应 | `application/xml`，UTF-8 **带 BOM**，约 3.7 KB |
| 无效 cityId | 返回 **HTTP 404** |
| 请求头 | 实测裸 `curl/8.0` UA 也能 200；但服务端会**间歇性 TLS 断连**（`UNEXPECTED_EOF_WHILE_READING`），**必须重试** |
| 特殊 cityId | `> 600000` 为 ECMWF 模式城市，数据文件为 `{cityId}_model_data_zh.json`，**无气候数据** |
| 城市索引 | `https://worldweather.wmo.int/zh/json/Country_zh.xml`（1.1 MB）→ 210 个成员国 / **3629 个城市**，可反查 cityId |

### 1.1 数据结构（cityId=1799 实测）

```
city
├── cityName / stationName / cityId / cityLatitude / cityLongitude
├── timeZone / isCapital / isDep
├── member { memId, memName, orgName, logo, url }   # 所属国家/地区
├── forecast { issueDate, forecastDay[] }           # 天气预报（本项目不用）
└── climate
    ├── raintype   # 'Rainfall' | 'PPT' | ''  ← 三种变体，实测均有出现
    ├── rainunit   # 'mm'，部分城市为空字符串
    ├── datab/datae         # 气温统计时段，如 1961 / 1990
    ├── tempb/tempe         # 气温记录时段
    ├── rdayb/rdaye         # 降水日数统计时段
    ├── rainfallb/rainfalle # 降水量统计时段
    ├── climatefromclino
    └── climateMonth[ n ]   # 实测均为 12 项，但解析不假设长度
        ├── month              1..12
        ├── minTemp            日均最低气温 °C（字符串）
        ├── maxTemp            日均最高气温 °C（字符串）
        ├── meanTemp          ← **实测 252/252 全为 null，必须由 (max+min)/2 计算**
        ├── minTempF/maxTempF/meanTempF   华氏
        ├── raindays           平均降水日数（字符串；实测存在空值）
        ├── rainfall           平均总降水量 mm（字符串；实测存在空值）
        └── climateFromMemDate 数据入库日期
```

### 1.2 实测数据质量（随机抽样 30 城）

- **无气候数据：9 / 30（30%）** → 必须有优雅降级（不画图、明确提示原因）
- `raintype` 分布：`Rainfall` 14、`PPT` 3、空 `''` 4 → **单位标题不能硬编码，需按 raintype 映射并有兜底**
- `meanTemp` 为空：**100%**（252/252 个月）
- `rainfall` 空值 12 处、`raindays` 空值 48 处 → **缺失值必须参与绘图与表格的空值处理**
- 月份数：**全部 12**（解析仍按通用长度处理）
- 存在负气温（北京 1 月均最低 −9.4 °C）→ 双轴 + 负值刻度必须正确处理

### 1.3 官方页面参考样式（用于拟定默认配色）

| 元素 | 官方取值 |
|---|---|
| 平均总降水 | 柱状，`#46cbd4`，**右轴** |
| 平均最高气温 | 平滑线（spline），`#eb6877` |
| 平均最低气温 | 平滑线（spline），`#0f91c4` |
| 平均气温 | 平滑线（spline），`#990000` |
| 画布底色 | `#ebf1f5` |
| 标题 | 城市名（Verdana 粗体 `#596679`） |
| 左轴标题 / 右轴标题 | `温度 (°C)` / `降水 (毫米)` |

默认配置将**沿用官方配色**，保证产出与该站观感一致。

### 1.4 环境结论

- Python：`3.13.12`（isolated 托管版）；已装 `openpyxl`、`Pillow`
- **缺** `matplotlib`/`numpy`/`requests`/`pyyaml` → 需在隔离环境安装
- PyPI 及清华镜像均可达（HTTP 200）
- 中文字体：`msyh.ttc`、`simhei.ttf`、`simsun.ttc` 均在 → 可避免中文乱码/方框

---

## 二、技术栈与目录结构

**语言**：Python 3.13 ｜ **绘图**：matplotlib ｜ **配置**：JSON（可选兼容 YAML）
**仅 1 个必需第三方依赖**（matplotlib，含 numpy）；HTTP 用标准库 `urllib`，零额外依赖

```
项目根/
├── wmo_climate.py            # CLI 主入口（唯一命令）
├── requirements.txt
├── config/
│   ├── profiles.yaml         # 多套配置（含 default + 若干预设风格）
│   └── README.md             # 配置项完整说明
├── src/
│   ├── http_client.py        # 请求：主请求头 / 超时 / 指数退避重试 / 编码(BOM)处理
│   ├── parser.py             # 解析 + 容错（空值、raintype 变体、无气候数据）
│   ├── city_index.py         # 城市索引（按名称反查 cityId，可选能力）
│   ├── config_loader.py      # 配置加载 / 深合并 / 继承 / 校验与报错
│   ├── table_writer.py       # 表格输出：CSV / Markdown / XLSX
│   ├── chart.py              # matplotlib 绘图（双轴 + 全参数可配）
│   └── models.py             # 数据模型（dataclass）
├── output/                   # 交付物：表格 + 图
├── tests/                    # 回归：北京/香港(有数据)、无数据城市、非法 ID
├── 过程文件/
│   ├── 侦察/                 # 已有：探测脚本 + 原始响应样本 + 报告
│   ├── 校验渲染/             # 渲染人工核对图（含回写后校验）
│   └── 中间产物/
└── 开发计划.md               # 本文件
```

> 遵循你的过程文件管理规范：交付物在 `output/` 与根目录，过程文件全部进 `过程文件/` 子目录并附 `说明.md`。

---

## 三、配置系统设计（重点）

### 3.1 多配置机制

- `config/profiles.yaml` 结构：顶层 `default_profile` + `styles` + `profiles` 三段（`profiles` 下为 `"<名称>": {...}`）
- **所有配置与内置默认值深合并**（deep-merge）→ 用户只需写要改的项，无需复制全量
- 支持 `extends` 继承：新配置可基于已有配置派生（如"大字号版 = 默认 + 字体放大"）
- 支持外部配置文件（`--profile-file`）不污染内置配置
- 命令行：`--profile <名称>`；**未指定则用 `default_profile`**
- `--list-profiles` 列出全部配置及说明；`--init-profile <名称>` 生成带注释的全量模板

内置预设（初定）：`default`（官方风格）、`compact`（紧凑小图）、`presentation`（大字号大画布，适合投屏）、`paper`（学术：白底、无网格、Times 风格）、`temp_only`（纯气温）、`rain_only`（纯降水）、`minimal`（无标题无图例）

### 3.2 完整配置项清单

> 已在你给出的示例（绘图元素、纵轴标题、标题位置、配色、字体、画布、是否显示标题）基础上补全。

#### A. `data` — 数据与字段口径
| 键 | 说明 |
|---|---|
| `lang` | 语言版本 `zh`/`en`/`fr`…，决定 URL 路径与月份标签语种 |
| `derive_mean_temp` | 数据源 `meanTemp` 恒空，是否用 `(min+max)/2` 派生（默认 true） |
| `derive_mean_temp_from_F` | 用华氏回算（不推荐，交叉验证用） |
| `temp_unit` | 输出温度单位 `C` / `F` |
| `rain_unit` | 输出降水单位 `mm` / `inch` |
| `fill_missing` | 缺失值策略：`gap`（断线）/ `zero`（补 0）/ `skip` |
| `include_year_column` | 表格与图表是否附年合计/年均列 |
| `annual_row_precision` | 年值小数位 |
| `month_label_style` | 月份标签样式：`1月` / `一月` / `Jan` / `01` |

#### B. `fetch` — 请求层
| 键 | 说明 |
|---|---|
| `base_url` | 站点根，便于将来换域名/镜像 |
| `city_page_path` | 页面路径模板 `city.html?cityId={city_id}` |
| `data_path` | 数据路径模板 `json/{city_id}_zh.xml`（含 `lang` 占位） |
| `headers` | **主请求头**：`User-Agent`、`Accept`、`Accept-Language`、`Referer`、`Connection`、`Cache-Control` 全可配 |
| `timeout` / `connect_timeout` | 超时 |
| `retries` / `backoff` / `backoff_max` | 重试次数与指数退避（应对实测的 TLS 偶发断连） |
| `verify_ssl` | 证书校验开关（默认 true，不建议关） |
| `proxy` | 代理 |
| `fetch_page_first` | 是否先请求 HTML 页面做存在性校验与元信息提取（默认 true） |
| `save_raw` | 是否把原始响应存到 `过程文件/中间产物/` |

#### C. `output` — 输出与文件
| 键 | 说明 |
|---|---|
| `out_dir` | 输出目录 |
| `name_template` | 文件名模板，支持 `{city}`、`{city_id}`、`{member}`、`{profile}`、`{ext}` |
| `table_formats` | 数组：`csv` / `md` / `xlsx` / `json`（多选） |
| `chart_formats` | 数组：`png` / `svg` / `pdf` |
| `chart_dpi` | 位图 DPI |
| `overwrite` | 覆盖策略：`overwrite` / `skip` / `timestamp`（加时间戳后缀） |
| `csv_encoding` / `csv_bom` | 编码，`utf-8-sig` 保证 Excel 直接打开不乱码 |
| `log_level` | 日志级别 |

#### D. `figure` — 画布与整体版式
| 键 | 说明 |
|---|---|
| `figsize` | `[宽, 高]`（英寸） |
| `dpi` | 画布 DPI |
| `facecolor` / `edgecolor` | 画布底色与边框 |
| `tight_layout` / `constrained_layout` / `subplots_adjust` | 布局与边距（`left/right/top/bottom/wspace`） |
| `font_family` | 字体族（默认 `Microsoft YaHei` 防中文方框） |
| `font_size` | 全局基准字号 |
| `font_weight` | 全局字重 |
| `axes_unicode_minus` | 负号显示为真减号 |
| `title.show` | **是否显示标题** |
| `title.text` | 标题文本模板，支持 `{city}`/`{member}`/`{city_id}`/`{station}` 占位 |
| `title.fontsize` / `color` / `fontweight` / `pad` / `loc` | 标题字体与位置（`left`/`center`/`right`） |
| `subtitle.show` / `text` / `fontsize` / `color` | 副标题 |
| `legend.show` / `loc` / `ncol` / `fontsize` / `frameon` / `framealpha` / `title` / `markerscale` / `columnspacing` | 图例全参数 |
| `grid.show` / `axis` / `color` / `linestyle` / `linewidth` / `alpha` / `which` | 网格 |
| `background.bands.show` / `season_colors` / `alpha` | 月份/季节背景色带 |
| `zeroline.show` / `color` / `linewidth` / `linestyle` | 零度基准线 |
| `annotation.show_extremes` | 标注最高/最低月 |
| `credit.show` / `text` | 数据来源署名（默认 WMO）

#### E. `axes_primary` / `axes_secondary` — 双轴
| 键 | 说明 |
|---|---|
| `show` | 是否启用该轴 |
| `side` | `left` / `right` |
| `label_text` | **纵轴标题**（如 `温度 (°C)`） |
| `label_position` | **纵轴标题位置**：`auto` / `left` / `center` / `right` + `label_x`/`label_y` 微调 |
| `label_fontsize` / `label_color` / `label_rotation` / `label_pad` | 轴标题样式 |
| `limit` | `[min, max]`，空则自动；支持 `auto_pad_ratio`（上下留白比例） |
| `tick_start` / `tick_step` / `tick_count` / `tick_round_to` | 刻度控制（起始/间隔/数量/取整） |
| `tick_format` | 刻度格式串（`{:.0f}` / `{:.1f}`） |
| `tick_fontsize` / `tick_color` / `tick_length` / `tick_direction` | 刻度样式（`in`/`out`/`inout`） |
| `scale` | `linear` / `log` |
| `negative_support` | 是否允许负值（默认 true） |
| `hide_upper_spine` / `spine_color` / `spine_linewidth` | 轴脊 |

#### F. `axes_x` — 横轴
| 键 | 说明 |
|---|---|
| `label_text` | 横轴标题（如 `月份`） |
| `label_fontsize` / `label_color` / `label_pad` | 样式 |
| `tick_rotation` / `tick_fontsize` / `tick_color` | 月份标签 |
| `tick_interval` | 隔 N 月显示一个标签（拥挤时用） |
| `show_month_gridline` | 月份分隔线 |

#### G. `series` — 绘图元素（**每个元素独立配置**）
每个元素一个字典，`key` 取值：`minTempC` / `maxTempC` / `meanTempC` / `rainfall` / `raindays`

| 键 | 说明 |
|---|---|
| `enabled` | **是否绘制该元素** |
| `label` | 图例名（`auto` 时按 raintype 自动命名，如 平均总降水/降水） |
| `chart_type` | `line` / `smooth`(样条平滑) / `bar` / `barh` / `area` / `step` / `scatter` / `fill_between`(区间带) |
| `axis` | 归属轴：`primary`(温度) / `secondary`(降水) |
| `color` | **元素配色**（支持 HEX / 名称 / 渐变列表） |
| `alpha` / `linewidth` / `linestyle` / `zorder` | 线条样式 |
| `marker` / `markersize` / `markerfacecolor` / `markeredgecolor` / `markevery` | 数据点 |
| `bar_width` / `bar_edgecolor` / `bar_linewidth` / `bar_label_position` | 柱状专有 |
| `smooth_points` | 平滑插值点数（`smooth` 专有） |
| `fill_alpha` | 区间带/面积图透明度 |
| `data_labels.show` / `fontsize` / `color` / `format` / `offset` | 数值标签 |
| `animate_over` | 是否为副坐标（如降水日数叠加于降水轴，可设独立缩放系数 `scale_factor`） |
| `order_in_legend` | 图例排序 |

#### H. `table` — 表格
| 键 | 说明 |
|---|---|
| `rows` | 行顺序与显示项（`minTemp`/`maxTemp`/`meanTemp`/`rainfall`/`raindays`），可增删排序 |
| `row_labels` | 行名文案覆盖 |
| `transpose` | 是否转置（月份为列 / 为行） |
| `precision` | 各列小数位 |
| `missing_placeholder` | 缺失占位符（默认 `—`） |
| `show_period_note` | 是否附统计时段脚注（取 `datab/datae`） |
| `note_text` | 自定义脚注（含数据来源声明） |
| `include_annual` | 是否含"年"列/行 |

#### I. `styles`（可选）— 命名样式复用
把一组 `series` + 字体 + 配色打包为命名样式，供多配置引用，避免重复。

---

## 四、分阶段实施计划

| 阶段 | 内容 | 产出与验收 |
|---|---|---|
| **P0** | 环境准备：隔离 venv 安装 matplotlib；中文字体在 matplotlib 中验证可渲染 | 一张中文测试图无方框 |
| **P1** | 请求层 `http_client.py`：主请求头、超时、指数退避重试、BOM 解码 | 对北京/香港稳定取数；模拟断连能自动重试成功 |
| **P2** | 解析层 `parser.py` + `models.py`：容错解析全部字段，派生平均气温 | 北京(PPT)/香港(Rainfall)/空 raintype 城市均正确 |
| **P3** | 配置系统 `config_loader.py` + `config/profiles.yaml`：默认值、深合并、extends、校验 | 只写 2 个字段的局部配置能正常生效 |
| **P4** | 表格输出 `table_writer.py` | CSV(UTF-8-BOM) + Markdown + XLSX 三格式正确 |
| **P5** | 绘图层 `chart.py`：双轴、全 series 参数、样式落地 | 默认配置出图与官方风格一致 |
| **P6** | CLI 整合 `wmo_climate.py`：`--city-id/--profile/--list-profiles/--init-profile/--out-dir` | 一条命令产出全套交付物 |
| **P7** | 内置多套预设配置并逐一渲染核对 | 每套配置一张出入图，放进 `过程文件/校验渲染/` |
| **P8** | 回归测试 + 边界用例（无气候数据、非法 ID、空值、负温度、大降水差值） | 全部用例有明确、不崩溃的输出 |
| **P9** | 文档：`README.md` + `config/README.md` 配置项完整字典；过程文件 `说明.md` | 交付说明齐全 |

**交付物**：可运行脚本 + 多套配置 + 示例输出（北京/香港等）+ 配置字典文档 + 回归测试

---

## 五、需要你确认的决策点

1. 绘图方案：matplotlib 静态图（PNG/SVG/PDF）是否满足？是否需要**额外**输出交互式 HTML 图（Plotly/ECharts，可悬停查看数值）？
2. 配置文件格式：JSON（零依赖）为主，是否同时支持 YAML？
3. 表格输出格式：CSV / Markdown / XLSX 需要哪几种？
4. 附加能力（多选）：城市名反查 cityId、一次批量多城市、多城市对比图、数据本地缓存、附带预报数据导出。

> 以上 4 点已于 2026-09-16 确认，结论见文件开头「已确认的技术决策」。预报数据导出未纳入本期范围。
