# WMO 城市气候工具 · 功能说明文档

> 本文档面向使用者与维护者，**逐一说明项目已实现的全部功能**。
>
> 相关文档分工：
> - **本文档**：功能全貌（有哪些能力、怎么用、边界如何）。
> - [`README.md`](README.md)：快速上手与参数速查。
> - [`config/README.md`](config/README.md)：配置项完整字典。
> - [`开发计划.md`](开发计划.md)：设计依据与决策记录。
>
> 版本：`wmo_climate 1.0.0` ｜ 语言：Python 3.10+（验证于 3.13）｜ 数据源：世界天气信息服务网（WMO）

---

## 1. 项目定位

按城市编号（`cityId`）解析 WMO（世界天气信息服务网）的城市**月气候平均值**，
生成**表格**（CSV / Markdown / XLSX / JSON）与**气温-降水统计图**（PNG / SVG / PDF / JPG / WEBP）。

核心特点：

- **全参数可配置**：图表元素、配色、双/三轴标题与位置、字体字号、画布尺寸、标题图例网格刻度、表格字段等全部可配。
- **多套配置并存**：内置 15 套预设 + 支持自定义、继承、样式复用，未指定时用默认配置。
- **零额外网络依赖**：HTTP 仅用标准库 `urllib`；唯一第三方依赖是 `matplotlib`（绘图）与 `openpyxl`（XLSX）。
- **容错优先**：针对数据源实测的各类缺失、变体、断连做了明确降级处理。

---

## 2. 功能总览

| 功能域 | 能力 | 关键实现 |
|---|---|---|
| 取数 | 城市页面存在性校验 + 气候数据文件抓取 | `src/http_client.py`、`src/pipeline.py` |
| 取数 | 指数退避重试、本地缓存、BOM 解码、代理、自定义请求头 | `src/http_client.py` |
| 解析 | 容错解析、平均气温派生、raintype/单位变体处理 | `src/parser.py`、`src/models.py` |
| 查询 | 城市名 → cityId 反查、模糊搜索、按国家列表 | `src/city_index.py` |
| 配置 | 三层来源、深合并、继承、样式复用、校验、模板导出 | `src/config_loader.py` |
| 表格 | CSV / Markdown / XLSX / JSON 四格式、转置、年列、脚注 | `src/table_writer.py` |
| 图表 | 单城双轴/三轴统计图、8 种图形类型、装饰元素 | `src/chart.py` |
| 图表 | 多城市对比图（气温/降水，曲线或柱状） | `src/chart.py` |
| 编排 | 单城、批量多城、对比三种模式，失败隔离 | `src/pipeline.py` |
| 命令行 | 目标/配置/输出/查询四组参数 | `wmo_climate.py` |
| 测试 | 130 项断言回归测试（离线可跑 + 可选联网；断言数随配置数量变化） | `tests/test_regression.py` |

---

## 3. 命令行功能

唯一入口：`wmo_climate.py`。参数分为四组。

### 3.1 目标城市

| 参数 | 功能 |
|---|---|
| `--city-id ID` | 指定城市编号，**可重复**传入实现批量 |
| `--city 名称` | 城市名称，自动反查 cityId，可重复 |
| `--compare ID或名称` | 多城市对比，可传编号或名称，可重复 |
| `--compare-metric 元素` | 对比元素：`minTemp`/`maxTemp`/`meanTemp`/`rainfall`/`raindays` |

### 3.2 配置

| 参数 | 功能 |
|---|---|
| `--profile 名称` | 选用配置；**未指定则用 `default_profile`** |
| `--profiles-file 路径` | 使用外部 profiles 文件，不污染项目内置配置 |
| `--set 键=值` | 点路径覆盖配置（值自动识别 JSON/数字/布尔），可重复 |
| `--list-profiles` | 列出全部可用配置及说明 |
| `--show-config` | 打印解析后的**最终配置**（便于确认合并结果） |
| `--init-profile 名称` | 导出全量配置模板到 `config/<名称>.json` |
| `--temp-unit C\|F` | 温度单位（等价于 `--set data.temp_unit=`） |
| `--rain-unit mm\|inch` | 降水单位（等价于 `--set data.rain_unit=`） |

### 3.3 输出

| 参数 | 功能 |
|---|---|
| `--out-dir 目录` | 输出目录（按当前工作目录解析） |
| `--no-chart` | 只出表格（等价于清空 `output.chart_formats`） |
| `--no-table` | 只出图（等价于清空 `output.table_formats`） |
| `--refresh-cache` | 忽略本地缓存强制重新请求 |
| `--refresh-index` | 强制重新拉取城市索引 |
| `--log-level LEVEL` | `DEBUG`/`INFO`/`WARNING`/`ERROR` |

### 3.4 城市编号查询

| 参数 | 功能 |
|---|---|
| `--search 关键词` | 按城市名搜索 cityId |
| `--list-cities` | 列出城市 |
| `--country 国家/地区` | 配合 `--list-cities` 按国家筛选 |
| `--limit N` | 列表/搜索结果条数上限（默认 50） |

### 3.5 退出码

| 码 | 含义 |
|---|---|
| `0` | 全部成功 |
| `1` | 部分城市失败（其余仍正常产出） |
| `2` | 配置错误 |
| `3` | 城市查询 / 网络失败 |

### 3.6 典型用法

```bash
# 默认配置出北京（cityId 237）的表格 + 图
python wmo_climate.py --city-id 237

# 按城市名反查并批量处理
python wmo_climate.py --city 北京 --city 香港

# 换配置 + 临时改参数
python wmo_climate.py --city-id 237 --profile presentation \
  --set series.rainfall.color=#ff7f0e --set figure.title.show=false

# 多城市对比（气温 / 降水）
python wmo_climate.py --compare 北京 --compare 1 --compare 156 --profile compare
python wmo_climate.py --compare 北京 --compare 香港 --profile compare_rain

# 查询城市编号
python wmo_climate.py --search 北京
python wmo_climate.py --list-cities --country 中国 --limit 100

# 配置查看与导出
python wmo_climate.py --list-profiles
python wmo_climate.py --show-config --profile paper
python wmo_climate.py --init-profile my_style
```

---

## 4. 数据源与取数功能

### 4.1 数据源

工具同时使用以下三个地址（路径模板均可在 `fetch` 配置中替换）：

| 用途 | 地址模板 |
|---|---|
| 城市页面（存在性校验、页面标题） | `{base_url}/{lang}/city.html?cityId={city_id}` |
| 气候数据（真正取数） | `{base_url}/{lang}/json/{city_id}_{lang}.xml` |
| 城市索引（名称反查） | `{base_url}/{lang}/json/Country_{lang}.xml` |

> 城市页面本身**不含气候数值**（HTML 仅含前端渲染脚本）；气候数据由脚本异步加载 `{city_id}_{lang}.xml`，
> 该文件**内容实为 JSON**，响应头标 `application/xml`，且带 UTF-8 BOM。

### 4.2 请求层能力（`http_client.py`）

| 能力 | 说明 |
|---|---|
| 主请求头 | `User-Agent` / `Accept` / `Accept-Language` / `Accept-Encoding` / `Connection` / `Cache-Control` / `Pragma` / `Upgrade-Insecure-Requests` 全可配，可追加自定义头 |
| 超时 | `fetch.timeout`（默认 30 秒） |
| 指数退避重试 | `fetch.retries`（默认 4）+ `fetch.backoff`（默认 1.2 秒，第 n 次等待 `backoff × 2^(n-1)`）+ `fetch.backoff_max`（默认 15 秒），应对**实测的间歇性 TLS 断连** |
| 可重试状态码 | `408 425 429 500 502 503 504` |
| 404 处理 | 立即判定为「城市不存在」，**不重试** |
| BOM 解码 | 按 `utf-8-sig → utf-8 → gb18030 → latin-1` 依次尝试 |
| 本地缓存 | 按 URL 的 SHA1 前 20 位存 `.bin` + `.meta.json`，可配 `cache.enabled` / `cache.ttl`（默认 1 天）/ `cache.dir`；命中时直接返回 |
| 原始响应落盘 | `fetch.save_raw=true` 时把响应存到 `raw_dir` |
| SSL / 代理 | `fetch.verify_ssl`（默认开启）、`fetch.proxy` |
| 请求统计 | 记录 `request_count` / `retry_count`，批量结束后输出 |

### 4.3 取数流程（单城）

1. （可选，`fetch_page_first=true`）请求城市 HTML 页做**存在性校验**并提取页面标题；
   页面校验失败**不阻断**——数据文件才是权威来源，仅告警后继续。
2. 请求气候数据文件，BOM 解码后解析。
3. 解析出 `CityClimate`；若数据源未提供平均气温则派生并记录日志。

---

## 5. 数据处理功能

### 5.1 解析容错（`parser.py` / `models.py`）

针对实测约束的处理清单：

| 实测现象 | 处理方式 |
|---|---|
| `climateMonth[].meanTemp` 恒为 `null`（抽样 252/252） | 由 `(minTemp + maxTemp) / 2` 派生，标记 `meanTempDerived`；可用 `data.derive_mean_temp` 关闭 |
| `raintype` 有 `Rainfall` / `PPT` / 空串 三种变体 | 按 raintype 映射元素名，并有兜底 |
| `rainunit` 可能为空串 | 回退为「毫米」 |
| `rainfall`、`raindays` 存在空值 | 统一为 `None`，按 `data.fill_missing` 策略处理；表格用占位符 |
| 约 30% 城市完全没有 `climate` 段 | 抛 `NoClimateDataError` 并给出明确原因，批量模式不中断其他城市 |
| 无效 cityId 返回 HTTP 404 | 识别为「城市不存在」，不重试 |
| `cityId > 600000` 为 ECMWF 模式城市 | 无气候数据，给出明确提示 |
| 外层结构可能是 `{"city": {...}}` 或直接对象 / 数组 | `unwrap_city` 兼容多种外层结构 |
| `climateMonth` 可能是字典 | 兼容转为列表处理 |

数值解析 `parse_number` 的容错：空串 / `null` / `N/A` / `—` / `nan` / `-` / `--` / `nil` / `undefined` 一律视为缺失；
支持去除千分位逗号与不换行空格；非数字文本返回 `None`。

### 5.2 单位换算与口径

- **单位换算不在解析阶段做**，温度同时保留摄氏/华氏原始值，输出时按 `data.temp_unit` 决定，避免精度损失。
- 温度：`C` / `F`（换算 `c*9/5+32`）。
- 降水：`mm` / `inch`（换算 `mm/25.4`）。
- **年值口径**：温度类元素取**有效月均值**，降水/降水日数取**有效月累加**（`CityClimate.annual`）。

### 5.3 月份标签样式

`data.month_label_style` 支持四种：`1月`（默认） / `一月` / `Jan` / `01`。

### 5.4 元素别名

`series` 与 `table.rows` 中的元素键支持别名，大小写不敏感：

| 规范键 | 可用别名 |
|---|---|
| `minTemp` | `minTempC`、`minTempF` |
| `maxTemp` | `maxTempC`、`maxTempF` |
| `meanTemp` | `meanTempC`、`meanTempF`、`avgTemp` |
| `rainfall` | `rain`、`precipitation`、`ppt` |
| `raindays` | `rainDays`、`rdays` |

---

## 6. 城市查询功能（`city_index.py`）

- **索引来源**：`Country_{lang}.xml`（实测约 1.1 MB，210 个成员国 / **3629 个城市**）。
- **归一化缓存**：首次拉取后压平为扁平列表缓存为 JSON，后续秒级加载；`--refresh-index` 可强制刷新。
- **按名反查**（`resolve_one`）：按匹配强度排序——完全相同 > 前缀 > 包含 > 英文匹配 > 近似（`difflib`）；首都与有坐标者加权优先；同名多城市时报歧义并给出候选清单。
- **搜索**（`search`）：返回按强度排序的候选列表。
- **按国家列表**（`list_by_country`）：支持精确与包含匹配。
- 命令行入口：`--city`、`--compare`（非数字项）、`--search`、`--list-cities`、`--country`、`--limit`。

---

## 7. 配置系统功能（`config_loader.py`）

### 7.1 配置来源（后者覆盖前者）

1. **内置默认值** `DEFAULTS`（含全部可配置项）。
2. **配置文件中的命名配置**：内置 `config/profiles.json`。
3. **自定义配置（多文件）**：`config/custom.json` + `config/custom/*.json` 按"上下顺序"加载并与内置配置合并（**同名时自定义优先**；可 `extends` 跨文件继承；未指定 `--profile` 时用 `default_profile`）。
4. **命令行 `--set 键=值`** 点路径覆盖。

### 7.2 合并与复用机制

| 机制 | 说明 |
|---|---|
| 深合并 | 对象递归合并；**数组整体替换**（非拼接），因此局部配置只需写想改的项 |
| `extends` | 配置继承另一套配置，**可链式、可跨文件**；检测到循环时报错 |
| `apply_styles` | 引用 `styles` 中定义的命名样式（如 `official_colors`、`big_font`、`clean_paper`） |
| 元素别名归一 | 配置与 `--set` 中的别名（`minTempC` 等）自动归一为规范键 |
| 外部文件 | `--profiles-file` 使用项目外配置，不污染内置配置 |
| 自定义配置 | 写在 `config/custom.json` 或 `config/custom/*.json`（多文件按文件名升序加载、后加载者覆盖先加载者），与内置配置合并共存（同名时自定义优先）；可 `extends` 跨文件继承 |

### 7.3 校验功能（`validate_config`）

- **致命错误**（抛 `ConfigError`，退出码 2）：`series` 未启用任何元素；非法的 `temp_unit` / `rain_unit`；`table.rows` 含无法识别的元素键；不支持的表格/图片格式；非法 `figure.layout`；配置继承循环；引用了不存在的配置或样式。
- **告警**（不中断）：出现未知配置项（提示可能拼错）；有元素用副轴但 `axes_secondary.show=false`。

### 7.4 模板导出与查看

- `--list-profiles`：列出全部配置及说明。
- `--show-config`：打印合并后的最终配置 JSON。
- `--init-profile <名称>`：导出**含全部可配置项**的模板到 `config/<名称>.json`，便于二次修改。

### 7.5 配置项分组（详见 `config/README.md`）

`data`（数据口径）· `fetch`（请求层）· `output`（输出文件）· `figure`（画布/标题/图例/网格/背景/零线/极值标注/署名）·
`axes_primary` / `axes_secondary` / `axes_tertiary`（三个纵轴）· `axes_x`（横轴）· `series`（每个绘图元素）·
`table`（表格）· `compare`（多城市对比）· `styles`（命名样式）。

---

## 8. 表格输出功能（`table_writer.py`）

### 8.1 支持格式

| `output.table_formats` 取值 | 扩展名 | 说明 |
|---|---|---|
| `csv` | `.csv` | UTF-8 BOM（`utf-8-sig`），Excel 直接打开不乱码 |
| `md` / `markdown` | `.md` | Markdown 表格 |
| `xlsx` | `.xlsx` | 需 `openpyxl`；带表头填色、隔行底色、边框、冻结窗格；**数值以真实数字类型写入**（可参与 Excel 计算，非文本），缺失值写占位符文本 |
| `json` | `.json` | 结构化全量数据 + 脚注 + 单位声明 |

空数组 = 不出表格。

### 8.2 表格口径与可配项

- **布局**：默认**行 = 元素，列 = 月份**；`table.transpose=true` 转置为月份为行。
- **行顺序与显示项**：`table.rows` 可增删排序；`table.row_labels` 覆盖行名（`rainfall` 用 `auto` 随 raintype 命名）。
- **年列/年行**：`table.include_annual` 追加（温度取月均、降水取累加），标签 `table.annual_label`（默认「年」）。
- **精度**：`table.precision.temp/.rainfall/.raindays/.annual`。
- **缺失值**：统一用 `table.missing_placeholder`（默认 `—`）。
- **单位**：`table.unit_in_label` 控制标签是否带单位。
- **脚注**：`show_period_note`（统计时段，取自 `datab/datae` 等）、`show_source_note`（数据来源）、`note_template` 模板。
- **标题行**：`show_title_row` 控制 CSV/XLSX/Markdown 顶部标题；`title_template` 定义标题文案。
- **XLSX 外观**：`table.xlsx.*` 可配工作表名、表头填充/字色、隔行底色、列宽、边框色、数字格式、是否冻结窗格。

---

## 9. 图表生成功能（`chart.py`）

采用 `matplotlib` 静态出图（`Agg` 后端），无 `scipy` 依赖，平滑曲线用自实现的三次 Hermite 插值。

### 9.1 单城统计图

- **双轴 / 三轴**：
  - 主轴 `axes_primary`（温度，默认左轴）；
  - 副轴 `axes_secondary`（降水，默认右轴）；
  - **第三轴 `axes_tertiary`**（如降水日数），相对右轴向外偏移 `offset_points`（默认 62 点），用于量级差异大的元素并置显示。
- **轴可配项**：是否启用、方位、轴标题文本/位置/旋转/字号/颜色/字重/间距、量程（手动或自动留白 `auto_pad_ratio`）、刻度（起始/间隔/数量/取整/格式/长度/方向/字号/颜色）、坐标尺度（`linear`/`log`）、轴脊显隐与样式。轴标题可横排（`label_rotation: 0`）并用 `label_x`/`label_y` 移到坐标轴之外（如置于轴上方），输出会将其纳入裁剪范围、不会被截断；配合 `label_align`（`left`/`center`/`right`）可与刻度标签左右对齐，`label_valign`（如 `bottom`）可让左右两轴标题处于同一行。
- **横轴 `axes_x`**：月份标题、标签旋转/字号/颜色、隔 N 月显示（`tick_interval`）、月份竖分隔线、左右留白。

### 9.2 绘图元素（`series`，每个元素独立配置）

可用元素：`minTemp`（日均最低气温）、`maxTemp`（日均最高气温）、`meanTemp`（日均气温）、`rainfall`（降水量）、`raindays`（降水日数）。

| 能力 | 说明 |
|---|---|
| 启用开关 | `enabled` 控制是否绘制 |
| 图形类型 | `line` / `smooth`（平滑）/ `bar`（柱）/ `area`（面积）/ `step`（阶梯）/ `scatter`（散点）/ `fill_between`（区间带） |
| 归属轴 | `axis` = `primary` / `secondary` / `tertiary` |
| 数值缩放 | `scale_factor` 把量级不同的元素压到同一轴（如降水日数 ×12） |
| 线条样式 | `color`、`alpha`、`linewidth`、`linestyle`、`zorder` |
| 数据点 | `marker`、`markersize`、`markerfacecolor`/`markeredgecolor`（`auto`=同色）、`markeredgewidth`、`markevery` |
| 柱状专有 | `bar_width`、`bar_edgecolor`、`bar_linewidth`、`bar_align` |
| 平滑专有 | `smooth_points`（插值点数，默认 240） |
| 区间带专有 | `fill_between_key`（另一边界元素，如 `maxTemp` 以 `minTemp` 为底）、`fill_alpha` |
| 数值标签 | `data_labels.show/fontsize/color/format/offset/rotation/position` |
| 图例顺序 | `order_in_legend`（绘制顺序固定为「**先柱状、后折线**」，与图例顺序无关） |
| 图层叠放 | **折线始终绘制在柱状之上**（跨坐标轴同样成立，如气温折线在主/三轴、降水柱在副轴时）；网格绘制在柱状之下 |

**各元素默认（官方页面配色）**：

| 元素 | 配色 | 类型 | 轴 | 默认启用 |
|---|---|---|---|---|
| `maxTemp` | `#eb6877` | smooth | primary | ✅ |
| `minTemp` | `#0f91c4` | smooth | primary | ✅ |
| `meanTemp` | `#990000` | line -- | primary | ❌ |
| `rainfall` | `#46cbd4` | bar | secondary | ✅ |
| `raindays` | `#0f6e56` | line -. | tertiary | ❌ |

### 9.3 版面与装饰元素

| 功能 | 配置项 |
|---|---|
| 画布 | 尺寸 `figsize`、DPI、底色 `facecolor`、绘图区底色 `axes_facecolor`（`auto` 时由底色向白混合派生）、边框色、布局 `tight`/`constrained`/`none`、手动边距 |
| 字体 | 字体族（按序回退，默认含 `Microsoft YaHei`/`SimHei` 防中文方框）、基准字号、字重、真减号 |
| 矢量文本 | SVG/PDF 中的文字保持**可编辑文本对象**：`figure.svg_fonttype`（`none` 输出 `<text>` / `path` 转轮廓）、`figure.pdf_fonttype`（`42` TrueType / `3` Type 3） |
| 标题/副标题 | 显隐、文本模板（支持占位符）、字号/颜色/字重/间距/对齐 |
| 图例 | 显隐、位置、锚点、列数、字号、边框与透明度、底色/边色、标题、标记缩放、列间距等 |
| 网格 | 显隐、方向（x/y/both）、主/次刻度、颜色/线型/线宽/透明度/层级；绘制在最底层，始终位于柱状图之下 |
| 背景色带 | 季节色带（可自定义季节与跨年月）或隔月交替（`background.bands.mode`）；绘制在最底层坐标轴，始终位于降水柱之下；南半球城市（纬度为负）自动反季（冬夏、春秋互换），可用 `background.bands.hemisphere` 指定 `auto`/`north`/`south` |
| 零基准线 | `zeroline` 显隐与样式 |
| 极值标注 | `annotation.show_extremes` 标注指定元素的最高/最低月，可含数值 |
| 数据来源署名 | `credit` 显隐、文本、位置、样式 |
| 缺失值策略 | `data.fill_missing`：`gap` 断线 / `zero` 补 0 / `skip` 跳过该点 |
| 自动降级 | `data.auto_disable_empty_series`：某元素在某城市全无数据时自动不绘制 |

### 9.4 文本占位符

用于 `title.text`、`subtitle.text`、`credit.text`、文件名等：

`{city}` 城市名 · `{city_id}` 编号 · `{member}` 国家/地区 · `{org}` 气象机构 · `{station}` 测站名 ·
`{period}` 统计时段 · `{lat}` `{lon}` 经纬度 · `{temp_unit}` 温度单位 · `{rain_unit}` 降水单位 ·
`{profile}` 配置名；对比图另支持 `{metric}`（对比元素名）与 `{city_count}`（城市数）。

---

## 10. 多城市对比功能（`chart.py` · `render_comparison_chart`）

- **触发**：`--compare` 传入 ≥1 个城市（编号或名称）。
- **对比元素**：`compare.metric` 或 `--compare-metric`，取值 `minTemp`/`maxTemp`/`meanTemp`/`rainfall`/`raindays`。
- **图形类型**：`compare.chart_type`（`smooth` 曲线或 `bar` 柱，柱宽按城市数均分）。
- **配色**：`colors` 序列 + `color_map`（城市名→颜色，优先）+ `color_by`（`order` 按顺序 / `city_id` 按编号取色）；线型与标记循环。
- **排序**：`sort_by` = `none`/`value_desc`/`value_asc`/`name`/`city_id`（按元素年值排序）。
- **图例**：`label_template` 图例名模板；`show_value_range` 附加该城市数值范围；`legend_ncol` 列数。
- **数值标签**：`compare.data_labels.*`。
- **标题**：`compare.title_text` 模板（默认 `{metric}对比 · {city}`）；副标题会列出各城市各自统计时段。
- **取数失败隔离**：个别城市取数失败不影响其余城市出图。

---

## 11. 批量与编排（`pipeline.py`）

| 模式 | 函数 | 说明 |
|---|---|---|
| 单城/批量 | `run_batch` | 逐个城市执行「取数 → 表格 → 图表」；**单城失败不影响其他城市** |
| 多城市对比 | `run_compare` | 汇总多个城市数据后统一出对比图，失败城市单独记录 |
| 城市名解析 | `resolve_city_ids` | 名称 → cityId |
| 城市查询 | `list_cities` | 搜索 / 按国家列出 |

- **输出文件名**：**图片**用 `output.name_template`（默认 `{city}_{city_id}_climate_{profile}`，即**以配置名作后缀**），
  **表格**用 `output.table_name_template`（默认 `{city}_{city_id}_climate`，**不带后缀**）；
  占位符 `{city}`/`{city_id}`/`{member}`/`{station}`/`{profile}`；对比图为 `output.compare_name_template`
  （默认 `{city_count}城对比_{metric}_{profile}`）。文件名非法字符自动替换为 `_`。
- **失败隔离**：表格写出失败不阻断绘图；绘图失败仅告警；结果汇总通过 `RunSummary.describe()` 输出成功/失败明细。

---

## 12. 输出与文件管理

| 能力 | 配置项 / 说明 |
|---|---|
| 输出目录 | `output.out_dir`（相对路径基于**项目根**解析）；`--out-dir` 按**当前工作目录**解析 |
| 文件命名 | **图片** `output.name_template`（默认带配置名后缀）/ **表格** `output.table_name_template`（默认不带后缀） |
| 表格格式 | `output.table_formats`：`csv`/`md`/`xlsx`/`json`（多选，空数组不出） |
| 图片格式 | `output.chart_formats`：`png`/`svg`/`pdf`/`jpg`/`webp`（多选，空数组不出） |
| 位图 DPI | `output.chart_dpi`（矢量格式不受影响） |
| 覆盖策略 | `output.overwrite`：`overwrite` 覆盖 / `skip` 跳过已存在 / `timestamp` 加时间戳后缀 |
| CSV 编码 | `output.csv_encoding`（默认 `utf-8-sig`） |
| 日志级别 | `output.log_level` |
| 运行信息 | 日志与结果汇总列出各城市**本次实际绘出要素**（已扣除禁用 / 所属轴关闭 / 无数据自动降级），对比模式给出「对比要素」 |
| 缓存目录 | `cache/`（`fetch.cache.dir`）：HTTP 响应缓存 + 城市索引 + `raw/` 原始响应，相对路径基于**项目根**解析，不入库 |
| 日志目录 | `logs/`：运行日志与脚本验收报告（如 `_cli_sweep.txt`） |

默认产出示例（见 `output/`）：表格 `北京_237_climate.{csv,md,xlsx}`（**不带**配置名后缀）；
图片 `北京_237_climate_default.png`、`北京_237_climate_简图.{png,svg}`、`3城对比_meanTemp_compare.png` 等（**图片以配置名作后缀**）。

---

## 13. 内置预设配置一览

| 配置 | 说明 |
|---|---|
| `default` | 官方页面风格：日均最低/最高气温 + 降水柱，双轴 |
| `full` | 全元素：最低/最高/平均气温 + 降水柱 + 降水日数（**第三轴**） |
| `compact` | 紧凑小图 |
| `presentation` | 投屏演示：大画布、大字号、粗线条、数据标签 |
| `paper` | 学术论文：白底、无图例边框、左对齐标题、附数据来源，输出 PDF/SVG |
| `temp_only` | 纯气温图（含最高最低之间的区间带） |
| `rain_only` | 纯降水图（降水量柱 + 降水日数折线，单轴） |
| `minimal` | 极简：无标题/图例/网格/轴标题 |
| `seasonal` | 季节底色带 + 气温极值月标注 |
| `alt_bands` | 隔月底色 |
| `yellow_black` | 高对比配色 |
| `annual_table` | 表格增强：年列、转置、含 JSON 输出 |
| `fahrenheit` | 英制单位（°F + 英寸） |
| `compare` | 多城市对比（气温） |
| `compare_rain` | 多城市对比（降水柱，继承 `compare`） |

可复用的命名样式：`official_colors`、`big_font`、`clean_paper`。

查看与导出：

```bash
python wmo_climate.py --list-profiles
python wmo_climate.py --init-profile my_style   # 导出全量模板后再改
```

自定义配置示例（只写想改的项，其余自动继承）：

```jsonc
{
  "profiles": {
    "my_style": {
      "extends": "default",
      "apply_styles": ["official_colors"],
      "figure": { "figsize": [14, 7], "title": { "text": "{city}气候 ({period})" } },
      "series": {
        "meanTemp": { "enabled": true, "chart_type": "smooth", "color": "#333333" },
        "rainfall": { "color": "#4a9fd8", "data_labels": { "show": true } }
      }
    }
  }
}
```

---

## 14. 测试与校验

```bash
python tests/test_regression.py            # 离线：用 tests/fixtures 中的真实响应样本
python tests/test_regression.py --network  # 追加联网用例
```

覆盖范围（130 项断言，随配置数量变化）：数值容错、平均气温派生、raintype 三种变体、缺失值、无气候数据城市、
配置深合并与继承、非法配置报错、四种表格格式与转置/年列/单位变体、
**全部配置逐一渲染**、三轴渲染、多城市对比、城市名反查。

可复用工具（`scripts/`）与过程脚本（`过程文件/`）：

| 能力 | 调用方式 |
|---|---|
| 抓取并更新测试夹具 | `python scripts/fetch_fixtures.py` |
| CLI 全子命令端到端验收 | `python scripts/cli_sweep.py` |
| 扫描样本缺失值分布 | `python scripts/scan_missing_values.py` |
| 接口侦察（字段完整性抽样） | `python 过程文件/侦察/recon_probe.py <Country_zh.xml路径> [抽样数]` |
| 边界行为侦察（404 / TLS 断连） | `python 过程文件/侦察/recon_probe2.py <Country_zh.xml路径>` |

---

## 15. 边界与降级策略

| 场景 | 行为 |
|---|---|
| 无气候数据城市（约 30%） | 抛出明确原因，批量模式不中断其他城市 |
| 无效 cityId（HTTP 404） | 判定为「城市不存在」，不重试 |
| ECMWF 模式城市（`cityId > 600000`） | 明确提示无气候数据 |
| 某元素在某城市全无数据 | `data.auto_disable_empty_series=true` 时自动不绘制该元素 |
| 降水全缺城市 | 自动降级为纯气温图 |
| 气温全缺城市 | 仅绘制降水相关元素 |
| 负气温 | 双轴 + 负值刻度正确处理，负号显示为真减号 |
| 含 0 值降水 | 柱状基线固定 0，正常渲染 |
| 间歇性 TLS 断连 | 指数退避自动重试 |
| 全部元素被关闭 | 配置校验阶段报错（退出码 2） |
| 表格写出失败 | 不阻断绘图，仅记录错误 |
| 绘图失败（如无任何可绘制元素） | 记录警告/错误，不中断批量流程 |

---

## 16. 目录结构与模块职责

```
DrawClimateChart/
├── wmo_climate.py            # CLI 主入口
├── requirements.txt          # matplotlib + openpyxl
├── 功能说明文档.md            # 本文档
├── README.md                 # 使用说明
├── 开发计划.md               # 设计依据与决策记录
├── config/
│   ├── profiles.json         # 内置配置（default + 14 套预设 + 命名样式）
│   ├── custom.json           # 自定义配置（集中存放，如「简图」）
│   └── README.md             # ★ 配置项完整字典
├── src/
│   ├── __init__.py           # 包版本
│   ├── http_client.py        # 请求层：请求头 / 超时 / 退避重试 / BOM 解码 / 缓存
│   ├── parser.py             # 解析层：容错 + 平均气温派生
│   ├── models.py             # 数据模型与单位换算
│   ├── city_index.py         # 城市名 → cityId 反查（3629 城）
│   ├── config_loader.py      # 配置：默认值 / 深合并 / 继承 / 样式 / 校验
│   ├── table_writer.py       # 表格：CSV / Markdown / XLSX / JSON
│   ├── chart.py              # 绘图：matplotlib 双/三轴，全参数可配
│   └── pipeline.py           # 编排：单城 / 批量 / 对比
├── scripts/                  # 可复用工具（夹具抓取 / CLI 验收 / 缺失值扫描）
├── tests/
│   ├── test_regression.py    # 回归测试
│   ├── fixtures/             # 真实响应样本（含 country_index_zh.json 与 samples/ 抽样数据）
│   └── _output/              # 测试产物：渲染核对图 / 表格中间输出（不入库）
├── output/                   # 交付物：表格与图（生成物，不入库）
├── cache/                    # 运行缓存：HTTP 响应 / 城市索引 / 原始响应（不入库）
├── logs/                     # 运行日志与验收报告
└── 过程文件/                  # 开发过程留证（侦察证据）
```

模块依赖方向：`http_client` → `parser`/`models` → `config_loader`/`city_index` → `table_writer`/`chart` → `pipeline` → `wmo_climate.py`。

---

## 17. 环境与安装

```bash
# 依赖：matplotlib（含 numpy）+ openpyxl；网络请求仅用标准库
pip install -r requirements.txt
```

- Python 3.10+（开发验证于 3.13）。
- 中文字体依赖系统字体，Windows 默认的 `Microsoft YaHei` / `SimHei` 已足够。

---

## 18. 已知限制

- 数据源只提供**月气候平均值**（气候态），不含逐年历史序列；`meanTemp` 需派生。
- 城市页面不含数据，因此「页面解析」实为**存在性校验 + 数据接口定位**，数值一律取自数据文件。
- 第三轴（降水日数）仅有右轴刻度，不参与主轴对齐，适合并置观感展示。
- `--compare` 只支持**单一元素**对比；不同城市统计时段可能不同，对比图副标题会列出各自时段。
- 绘图层中 `fill_missing` 的 `gap` 与 `skip` 效果一致（均留空为断点），仅语义不同。
- 预报数据（`forecast` 段）未纳入本项目范围。
